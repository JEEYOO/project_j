from sklearn.datasets import load_iris
iris = load_iris()

iris
print(iris)

type(iris)
iris.keys()    #dict_keys(['data', 'target', 'frame', 'target_names', 'DESCR', 'feature_names', 'filename', 'data_module'])

type(iris.data)
iris.data[:5]
iris.data.shape
iris.feature_names

print([iris.target_names[target] for target in iris.target])


import numpy

x10 = numpy.array([10, 9, 3, 2])
x10

import matplotlib.pyplot as plt

plt.figure(figsize=(10,5))

plt.subplot(1,2,1)
